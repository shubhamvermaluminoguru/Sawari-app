import { StyleSheet } from "react-native";

export const light = StyleSheet.create({
    primaryBg: {backgroundColor:'#fff'},
    secondaryBg: {backgroundColor:'#fff'},
})

export const dark = StyleSheet.create({
    
})